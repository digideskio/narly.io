!!!
%html
  %head
    %meta{ charset: "utf-8" }
    %meta{ name: "viewport", content: "width=device-width, initial-scale=1.0" }
  %body
    %ul
    - plusjade.all.each do |p|
      %li
        %a{href: p.url }= p.id.gsub('.haml', '')
